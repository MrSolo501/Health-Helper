import 'package:flutter/material.dart';

class EdaScreenWidget extends StatelessWidget {
  const EdaScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
