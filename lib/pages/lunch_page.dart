import 'package:flutter/material.dart';


class lunchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Обед')),

    );
  }
}